NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only. Powers the AI coach interview (/coach/playbook/interview).
# Without it the interview page says so plainly rather than faking intelligence.
# Never prefix this with NEXT_PUBLIC_ — that would ship the key to browsers.
ANTHROPIC_API_KEY=

# Automated tests only. `mock` swaps the provider for scripted, clearly-labelled
# responses. Leave unset everywhere else.
# READREP_AI_MODE=mock
