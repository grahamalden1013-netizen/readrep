# Next.js and its native binaries resolve more reliably with a hoisted layout
# for the framework packages only; everything else stays strictly isolated so
# workspace packages cannot import dependencies they do not declare.
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
strict-peer-dependencies=false
