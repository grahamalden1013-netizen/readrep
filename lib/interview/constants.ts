/**
 * Shared constants. Kept out of the `"use server"` modules, which may only
 * export async functions.
 */

/** What a coach must type to clear everything the interview learned. */
export const RESET_PHRASE = "start over";
